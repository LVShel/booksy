spring:
  application:
    name: booksy
  datasource:
    url: jdbc:h2:mem:bookstoredb
    driverClassName: org.h2.Driver
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
